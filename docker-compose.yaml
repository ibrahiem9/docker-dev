version: '3'

services:
  workspace:
    image: ubuntu-local
    ports:
      - 8080:8080
    volumes:
      - ~/workspace:/root/workspace
    working_dir: /root/workspace
    stdin_open: true
    tty: true